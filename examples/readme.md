# Ness.js examples

- [`welcome`](./welcome) — the end-to-end reference application used to verify React routing, NestJS controllers, server loaders, middleware, metadata, caching, production builds, and deployment.

Run it from the repository root:

```bash
npm run dev --workspace welcome
```
