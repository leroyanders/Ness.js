# `ness info`

Prints environment information useful for diagnostics and bug reports.

```bash
ness info
ness --info
```

The output includes the operating system, CPU, Node.js, npm, Yarn, installed Ness packages, globally installed CLI, and detected browser versions. The top-level `--info` option is retained for compatibility; both forms produce the same report.
